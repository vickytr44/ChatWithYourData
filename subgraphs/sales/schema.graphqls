schema {
  query: Query
  mutation: Mutation
}

type Query {
  customers(
    "Returns the first _n_ elements from the list."
    first: Int
    "Returns the elements in the list that come after the specified cursor."
    after: String
    "Returns the last _n_ elements from the list."
    last: Int
    "Returns the elements in the list that come before the specified cursor."
    before: String
    where: CustomerFilterInput @cost(weight: "10")
    order: [CustomerSortInput!] @cost(weight: "10")
  ): CustomerConnection!
    @listSize(
      assumedSize: 150
      slicingArguments: ["first", "last"]
      slicingArgumentDefaultValue: 25
      sizedFields: ["edges", "nodes"]
      requireOneSlicingArgument: false
    )
    @cost(weight: "10")
  customerById(id: UUID!): Customer @lookup @cost(weight: "10")
  salesOrders(
    "Returns the first _n_ elements from the list."
    first: Int
    "Returns the elements in the list that come after the specified cursor."
    after: String
    "Returns the last _n_ elements from the list."
    last: Int
    "Returns the elements in the list that come before the specified cursor."
    before: String
    where: SalesOrderFilterInput @cost(weight: "10")
    order: [SalesOrderSortInput!] @cost(weight: "10")
  ): SalesOrderConnection!
    @listSize(
      assumedSize: 150
      slicingArguments: ["first", "last"]
      slicingArgumentDefaultValue: 25
      sizedFields: ["edges", "nodes"]
      requireOneSlicingArgument: false
    )
    @cost(weight: "10")
  salesOrderById(id: UUID!): SalesOrder @lookup @cost(weight: "10")
  shipments(
    "Returns the first _n_ elements from the list."
    first: Int
    "Returns the elements in the list that come after the specified cursor."
    after: String
    "Returns the last _n_ elements from the list."
    last: Int
    "Returns the elements in the list that come before the specified cursor."
    before: String
    where: ShipmentFilterInput @cost(weight: "10")
    order: [ShipmentSortInput!] @cost(weight: "10")
  ): ShipmentConnection!
    @listSize(
      assumedSize: 150
      slicingArguments: ["first", "last"]
      slicingArgumentDefaultValue: 25
      sizedFields: ["edges", "nodes"]
      requireOneSlicingArgument: false
    )
    @cost(weight: "10")
  shipmentById(id: UUID!): Shipment @lookup @cost(weight: "10")
}

type Mutation {
  createCustomer(input: CreateCustomerInput!): CreateCustomerPayload!
    @cost(weight: "10")
  createSalesOrder(input: CreateSalesOrderInput!): CreateSalesOrderPayload!
    @cost(weight: "10")
}

type CreateCustomerPayload {
  customer: Customer
  errors: [CreateCustomerError!]
}

type CreateSalesOrderPayload {
  salesOrder: SalesOrder
  errors: [CreateSalesOrderError!]
}

type Customer @key(fields: "id") {
  customerNumber: String!
  name: String!
  email: String!
  phone: String!
  billingAddress: String!
  shippingAddress: String!
  creditLimit: Decimal!
  isActive: Boolean!
  id: UUID!
  createdAtUtc: DateTime!
  updatedAtUtc: DateTime
}

"A connection to a list of items."
type CustomerConnection @shareable {
  "A list of edges."
  edges: [CustomerEdge!]
  "A flattened list of the nodes"
  nodes: [Customer!]
  "Information to aid in pagination."
  pageInfo: PageInfo!
  "Identifies the total count of items in the connection."
  totalCount: Int! @cost(weight: "10")
}

type CustomerEdge @shareable {
  "The item at the end of the edge."
  node: Customer!
  "A cursor for use in pagination."
  cursor: String!
}

type CustomerNotFoundError implements Error {
  message: String!
  customerId: UUID!
}

type CustomerNumberAlreadyExistsError implements Error {
  message: String!
  customerNumber: String!
}

"A cursor that points to a specific page."
type PageCursor @shareable {
  "The page number."
  page: Int!
  "The cursor."
  cursor: String!
}

"Information about pagination in a connection."
type PageInfo @shareable {
  "Indicates whether more edges exist following the set defined by the clients arguments."
  hasNextPage: Boolean!
  "Indicates whether more edges exist prior the set defined by the clients arguments."
  hasPreviousPage: Boolean!
  "When paginating backwards, the cursor to continue."
  startCursor: String
  "When paginating forwards, the cursor to continue."
  endCursor: String
  "A list of cursors to continue paginating forwards."
  forwardCursors: [PageCursor!]!
  "A list of cursors to continue paginating backwards."
  backwardCursors: [PageCursor!]!
}

type Product {
  id: UUID!
}

type SalesOrder @key(fields: "id") {
  customer: Customer @cost(weight: "10")
  lines: [SalesOrderLine!]! @cost(weight: "10")
  orderNumber: String!
  customerId: UUID!
  orderDateUtc: DateTime!
  status: OrderStatus!
  totalAmount: Decimal!
  notes: String!
  id: UUID!
  createdAtUtc: DateTime!
  updatedAtUtc: DateTime
}

"A connection to a list of items."
type SalesOrderConnection @shareable {
  "A list of edges."
  edges: [SalesOrderEdge!]
  "A flattened list of the nodes"
  nodes: [SalesOrder!]
  "Information to aid in pagination."
  pageInfo: PageInfo!
  "Identifies the total count of items in the connection."
  totalCount: Int! @cost(weight: "10")
}

type SalesOrderEdge @shareable {
  "The item at the end of the edge."
  node: SalesOrder!
  "A cursor for use in pagination."
  cursor: String!
}

type SalesOrderLine {
  product: Product!
  salesOrderId: UUID!
  productId: UUID!
  sku: String!
  productName: String!
  quantity: Int!
  unitPrice: Decimal!
  discountPercentage: Decimal!
  lineTotal: Decimal!
  id: UUID!
  createdAtUtc: DateTime!
  updatedAtUtc: DateTime
}

type Shipment @key(fields: "id") {
  salesOrder: SalesOrder @cost(weight: "10")
  salesOrderId: UUID!
  trackingNumber: String!
  carrier: String!
  status: ShipmentStatus!
  shippedAtUtc: DateTime
  deliveredAtUtc: DateTime
  id: UUID!
  createdAtUtc: DateTime!
  updatedAtUtc: DateTime
}

"A connection to a list of items."
type ShipmentConnection @shareable {
  "A list of edges."
  edges: [ShipmentEdge!]
  "A flattened list of the nodes"
  nodes: [Shipment!]
  "Information to aid in pagination."
  pageInfo: PageInfo!
  "Identifies the total count of items in the connection."
  totalCount: Int! @cost(weight: "10")
}

type ShipmentEdge @shareable {
  "The item at the end of the edge."
  node: Shipment!
  "A cursor for use in pagination."
  cursor: String!
}

interface Error {
  message: String!
}

union CreateCustomerError = CustomerNumberAlreadyExistsError

union CreateSalesOrderError = CustomerNotFoundError

input BooleanOperationFilterInput {
  eq: Boolean @cost(weight: "10")
  neq: Boolean @cost(weight: "10")
}

input CreateCustomerInput {
  name: String!
  email: String!
  phone: String!
  billingAddress: String!
  shippingAddress: String!
  creditLimit: Decimal!
}

input CreateSalesOrderInput {
  customerId: UUID!
  notes: String!
  lines: [CreateSalesOrderLineInput!]!
}

input CreateSalesOrderLineInput {
  productId: UUID!
  sku: String!
  productName: String!
  quantity: Int!
  unitPrice: Decimal!
  discountPercentage: Decimal!
}

input CustomerFilterInput {
  and: [CustomerFilterInput!]
  or: [CustomerFilterInput!]
  name: StringOperationFilterInput
  customerNumber: StringOperationFilterInput
  email: StringOperationFilterInput
  isActive: BooleanOperationFilterInput
}

input CustomerSortInput {
  name: SortEnumType @cost(weight: "10")
  customerNumber: SortEnumType @cost(weight: "10")
  createdAtUtc: SortEnumType @cost(weight: "10")
}

input DateTimeOperationFilterInput {
  eq: DateTime @cost(weight: "10")
  neq: DateTime @cost(weight: "10")
  in: [DateTime] @cost(weight: "10")
  nin: [DateTime] @cost(weight: "10")
  gt: DateTime @cost(weight: "10")
  ngt: DateTime @cost(weight: "10")
  gte: DateTime @cost(weight: "10")
  ngte: DateTime @cost(weight: "10")
  lt: DateTime @cost(weight: "10")
  nlt: DateTime @cost(weight: "10")
  lte: DateTime @cost(weight: "10")
  nlte: DateTime @cost(weight: "10")
}

input OrderStatusOperationFilterInput {
  eq: OrderStatus @cost(weight: "10")
  neq: OrderStatus @cost(weight: "10")
  in: [OrderStatus!] @cost(weight: "10")
  nin: [OrderStatus!] @cost(weight: "10")
}

input SalesOrderFilterInput {
  and: [SalesOrderFilterInput!]
  or: [SalesOrderFilterInput!]
  orderNumber: StringOperationFilterInput
  customerId: UuidOperationFilterInput
  status: OrderStatusOperationFilterInput
  orderDateUtc: DateTimeOperationFilterInput
}

input SalesOrderSortInput {
  orderNumber: SortEnumType @cost(weight: "10")
  orderDateUtc: SortEnumType @cost(weight: "10")
  totalAmount: SortEnumType @cost(weight: "10")
}

input ShipmentFilterInput {
  and: [ShipmentFilterInput!]
  or: [ShipmentFilterInput!]
  salesOrderId: UuidOperationFilterInput
  trackingNumber: StringOperationFilterInput
  status: ShipmentStatusOperationFilterInput
}

input ShipmentSortInput {
  trackingNumber: SortEnumType @cost(weight: "10")
  shippedAtUtc: SortEnumType @cost(weight: "10")
}

input ShipmentStatusOperationFilterInput {
  eq: ShipmentStatus @cost(weight: "10")
  neq: ShipmentStatus @cost(weight: "10")
  in: [ShipmentStatus!] @cost(weight: "10")
  nin: [ShipmentStatus!] @cost(weight: "10")
}

input StringOperationFilterInput {
  and: [StringOperationFilterInput!]
  or: [StringOperationFilterInput!]
  eq: String @cost(weight: "10")
  neq: String @cost(weight: "10")
  contains: String @cost(weight: "20")
  ncontains: String @cost(weight: "20")
  in: [String] @cost(weight: "10")
  nin: [String] @cost(weight: "10")
  startsWith: String @cost(weight: "20")
  nstartsWith: String @cost(weight: "20")
  endsWith: String @cost(weight: "20")
  nendsWith: String @cost(weight: "20")
}

input UuidOperationFilterInput {
  eq: UUID @cost(weight: "10")
  neq: UUID @cost(weight: "10")
  in: [UUID] @cost(weight: "10")
  nin: [UUID] @cost(weight: "10")
  gt: UUID @cost(weight: "10")
  ngt: UUID @cost(weight: "10")
  gte: UUID @cost(weight: "10")
  ngte: UUID @cost(weight: "10")
  lt: UUID @cost(weight: "10")
  nlt: UUID @cost(weight: "10")
  lte: UUID @cost(weight: "10")
  nlte: UUID @cost(weight: "10")
}

enum OrderStatus {
  DRAFT
  CONFIRMED
  PROCESSING
  SHIPPED
  COMPLETED
  CANCELLED
}

enum ScalarSerializationType {
  STRING
  BOOLEAN
  INT
  FLOAT
  OBJECT
  LIST
}

enum ShipmentStatus {
  PENDING
  IN_TRANSIT
  DELIVERED
  RETURNED
}

enum SortEnumType {
  ASC
  DESC
}

"The `DateTime` scalar type represents a date and time with time zone offset information."
scalar DateTime
  @serializeAs(
    type: STRING
    pattern: "^\\d{4}-\\d{2}-\\d{2}[Tt]\\d{2}:\\d{2}:\\d{2}(?:\\.\\d{1,9})?(?:[Zz]|[+-]\\d{2}:\\d{2})$"
  )
  @specifiedBy(url: "https://scalars.graphql.org/chillicream/date-time.html")

"The `Decimal` scalar type represents a decimal floating-point number with high precision."
scalar Decimal
  @serializeAs(type: FLOAT)
  @specifiedBy(url: "https://scalars.graphql.org/chillicream/decimal.html")

scalar FieldSelectionSet

"The `UUID` scalar type represents a Universally Unique Identifier (UUID) as defined by RFC 9562."
scalar UUID
  @serializeAs(
    type: STRING
    pattern: "^[\\da-fA-F]{8}-[\\da-fA-F]{4}-[\\da-fA-F]{4}-[\\da-fA-F]{4}-[\\da-fA-F]{12}$"
  )
  @specifiedBy(url: "https://scalars.graphql.org/chillicream/uuid.html")

"The purpose of the `cost` directive is to define a `weight` for GraphQL types, fields, and arguments. Static analysis can use these weights when calculating the overall cost of a query or response."
directive @cost(
  "The `weight` argument defines what value to add to the overall cost for every appearance, or possible appearance, of a type, field, argument, etc."
  weight: String!
) on
  | SCALAR
  | OBJECT
  | FIELD_DEFINITION
  | ARGUMENT_DEFINITION
  | ENUM
  | INPUT_FIELD_DEFINITION

"""
The @key directive is used to designate an entity's unique key,
which identifies how to uniquely reference an instance of
an entity across different source schemas.
"""
directive @key("The field selection set syntax." fields: FieldSelectionSet!) repeatable on
  | OBJECT
  | INTERFACE

"The purpose of the `@listSize` directive is to either inform the static analysis about the size of returned lists (if that information is statically available), or to point the analysis to where to find that information."
directive @listSize(
  "The `assumedSize` argument can be used to statically define the maximum length of a list returned by a field."
  assumedSize: Int
  "The `slicingArguments` argument can be used to define which of the field's arguments with numeric type are slicing arguments, so that their value determines the size of the list returned by that field. It may specify a list of multiple slicing arguments."
  slicingArguments: [String!]
  "The `slicingArgumentDefaultValue` argument can be used to define a default value for a slicing argument, which is used if the argument is not present in a query."
  slicingArgumentDefaultValue: Int
  "The `sizedFields` argument can be used to define that the value of the `assumedSize` argument or of a slicing argument does not affect the size of a list returned by a field itself, but that of a list returned by one of its sub-fields."
  sizedFields: [String!]
  "The `requireOneSlicingArgument` argument can be used to inform the static analysis that it should expect that exactly one of the defined slicing arguments is present in a query. If that is not the case (i.e., if none or multiple slicing arguments are present), the static analysis may throw an error."
  requireOneSlicingArgument: Boolean = true
) on FIELD_DEFINITION

"""
The @lookup directive is used within a source schema to specify output fields
that can be used by the distributed GraphQL executor to resolve an entity by
a stable key.
"""
directive @lookup on FIELD_DEFINITION

directive @serializeAs(
  "The primitive type a scalar is serialized to."
  type: [ScalarSerializationType!]!
  "The ECMA-262 regex pattern that the serialized scalar value conforms to."
  pattern: String
) on SCALAR

directive @shareable repeatable on OBJECT | FIELD_DEFINITION